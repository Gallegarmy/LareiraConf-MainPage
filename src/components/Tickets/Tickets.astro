---
import Wrapper from "@components/Others/Wrapper.astro";
import Ticket from "@img/assets/ticket.webp";
import { Image } from "astro:assets";
---

<Wrapper>
  <div class="center grid grid-cols-1 gap-12 text-center lg:grid-cols-2">
    <div class="flex items-center justify-center">
      <Image
        class="ticket"
        src={Ticket}
        alt={"imagen de una entrada"}
        id="ticket-front"
      />
      <Image
        class="ticket"
        src={Ticket}
        alt={"imagen de una entrada"}
        id="ticket-back"
      />
    </div>
    <div class="flex flex-col items-center justify-evenly gap-6 lg:w-fit">
      <h1 class="text-left font-title text-3xl font-bold xl:text-5xl">
        ¡Entradas agotadas!
      </h1>
      <div>
        Gracias a todos, por hacerlo posible. Nos vemos junto al fuego 🔥
      </div>
      <!-- <ul class="text-left">
        <li>🎟️ Acceso al evento completo.</li>
        <li>🎁 Welcome pack.</li>
        <li>🗣️ Más de 10 charlas con ponentes top.</li>
        <li>⏰ 12h de evento non stop.</li>
        <li>☕ Coffee breaks y comida.</li>
        <li>🎉 Sorteos y merchan.</li>
        <li>💼 Conexión con empresas del sector.</li>
        <li>🎊 ¡Y muchas sorpresas más!</li>
      </ul> -->
      <!-- <div class="flex justify-left lg:w-full">
        <a
          class="btn-3d main-btn fire"
          target="_blank"
          href="https://lareiraconf.eventbrite.es">Cómprala ya</a
        >
      </div>
    </div> -->
    </div>
    <div class="game-advice w-full border-8 text-center md:w-1/3">
      <div class="tickets flex items-center justify-center border-8">
        <Image
          class="ticket"
          src={Ticket}
          alt={"imagen de una entrada"}
          id="ticket-front"
        />
        <Image
          class="ticket"
          src={Ticket}
          alt={"imagen de una entrada"}
          id="ticket-back"
        />
      </div>
      <h2 class="text-xl">¿Quieres ganar una entrada?</h2>
      <a class="underline" href="/game">Participa en el sorteo</a>
    </div>
  </div>

  <style>
    .ticket {
      width: 350px;
      height: fit-content;
      filter: drop-shadow(0 0 2px rgba(0, 0, 0, 0.5));
      transition: transform 0.3s ease-in-out;
    }

    @keyframes rotateAnimation {
      0% {
        transform: rotate(5deg);
      }
      50% {
        transform: rotate(2deg);
      }
      100% {
        transform: rotate(5deg);
      }
    }

    #ticket-front {
      position: relative;
      rotate: 10deg;
    }
    #ticket-back {
      position: absolute;
      animation: rotateAnimation 5s infinite;
    }
  </style>
</Wrapper>
