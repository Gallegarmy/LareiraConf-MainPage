# Variables de entorno para el sorteo (lado servidor Astro)
# Copia este archivo a .env y rellena los valores reales (NO commitear .env)

# ID del spreadsheet de Google Sheets (parte entre /d/ y /edit en la URL)
GOOGLE_SHEETS_ID=

# Credenciales OAuth (aplicación Google Cloud con API Sheets habilitada)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# Opcional: si quieres usar API Key para operaciones simples (no usada actualmente)
# GOOGLE_SHEETS_API_KEY=

# (Nota) El endpoint usa la hoja 'trg' fija. Puedes cambiar sheetName en src/pages/api/raffle.ts si necesitas otra.
